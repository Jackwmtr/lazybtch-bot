# Bot token from @BotFather (NEVER commit real values)
BOT_TOKEN=

# Fernet master key for encrypting user Groq keys:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Group transcription policy: off | mentions | all (default: mentions)
GROUP_POLICY=mentions

# Optional: port for future webhook/UI (long polling needs no port)
# LAZYBTCH_PORT=8321
